# 工作进度记录

## 2026-05-22

### [MINOR] 创建课程设计要求文档
**操作**：从用户提供的4张图片中提取内容，创建 `课程设计要求.md`

**原因**：用户要求整理老师提供的课程设计文档，需要将图片中的文字内容转换为结构化的 Markdown 文档，方便后续查阅和使用。

**状态**：已完成初稿

### [MINOR] 修正文档内容
**操作**：修正 `课程设计要求.md`，删除了"步骤2"中重复的内容

**原因**：用户指出"步骤2"和"方案一"内容重复，我在第一次整理时把两部分内容混淆了，需要删除重复部分。

**状态**：部分修正

### [MINOR] 重新整理完整文档
**操作**：完全重写 `课程设计要求.md`，正确区分"步骤2：后端开发"和"方案一：高性价比流"

**原因**：之前把两部分内容混淆了，需要重新从图片中提取正确内容。"步骤2"应该只包含后端开发的内容（数据库设计、数据入库、API开发），而"方案一"才是完整的前端技术栈说明（Flask、HTML、Tailwind、jQuery）。

**状态**：已完成

**文件内容**：
- 第一阶段：选题与数据准备
- 第二阶段：小组分工（3个角色）
- 第三阶段：核心开发步骤（步骤1和步骤2）
- 前端开发方案（方案一、二、三）

### [MINOR] 会话初始化检查（第一阶段）
**操作**：执行会话初始化 skill，检查项目配置

**原因**：用户调用了会话初始化 skill，需要检查当前项目是否已有完整的 hooks 配置，确保所有强制规则（skill 检查、过程文件、头脑风暴等）都已就位。

**检查结果**：
- 项目级 `.claude/settings.json` 已存在，包含完整的 29 个 hook 配置
- PostToolBatch、TaskCompleted、PreCompact、Stop 均配置了阻断脚本
- 所有强制规则 hook 已就位

**状态**：第一阶段初始化检查完成

---

### [MAJOR] 头脑风暴：确定选题方向
**操作**：通过头脑风暴与用户讨论选题，最终确定项目主题

**决策过程**：
- **问题分析**：需要选择一个符合课程要求的项目主题（数据量≥50000条，字段≥8个，适合机器学习和可视化），同时要避免与其他同学重复。用户反馈电商和共享单车已有同学使用，需要寻找其他方向。
- **备选方案**：
  - 方案一：酒店预订分析系统（Kaggle "Hotel Booking Demand"，119,390条记录，32个字段）
  - 方案二：航班延误预测系统（美国航班数据，百万级记录）
  - 方案三：外卖订单分析系统（餐饮订单数据）
  - 方案四：信用卡欺诈检测系统（28万+条交易记录）
  - 方案五：电影评分推荐系统（MovieLens，百万级评分）
  - 方案六：空气质量监测系统（时序数据，环境监测）
  - 方案七：二手车价格预测系统（数十万条记录）
- **对比维度**：数据可获取性、数据质量、业务场景清晰度、机器学习任务明确性、可视化效果、与同学重复风险
- **最终决策**：选择方案一（酒店预订分析系统）。理由：数据集质量高且容易从 Kaggle 直接下载，字段丰富（32个）远超要求，业务场景清晰（预订取消预测），有明确的机器学习任务（二分类），可视化维度多样（时间趋势、地区分布、客户画像），且没有同学使用。放弃其他方案主要因为数据获取难度较大或业务场景不够直观。

**数据集**：Kaggle "Hotel Booking Demand"（119,390条记录，32个字段）

**状态**：已确定选题，等待用户下载数据集

---

### [MINOR] 数据集下载与初步探索
**操作**：用户下载 Kaggle "Hotel Booking Demand" 数据集，使用 Python pandas 读取并查看基本信息

**原因**：老师要求先拿到数据再做下一步，需要确认数据集的实际情况（字段、数据量、缺失值）为后续功能设计提供依据。

**发现**：
- 数据量：119,390条记录，32个字段（远超课程要求的≥50000条、≥8个字段）
- 关键字段：`is_canceled`（取消标签0/1）、`lead_time`（提前天数）、`adr`（房价）、`country`（国家）、`hotel`（酒店类型）等
- 缺失值：`company`字段缺失严重（112,593条），`agent`字段缺失16,340条，其他字段缺失较少
- 数据类型：包含数值型和分类型，适合机器学习

**状态**：已完成数据下载和初步探索

---

### [MAJOR] 确定核心功能方向
**操作**：基于数据集实际情况，与用户讨论并确定系统的核心功能方向

**决策过程**：
- **问题分析**：数据集有明确的 `is_canceled` 标签字段（37%预订被取消），非常适合做分类预测。同时有丰富的数值和分类特征，可以做多维度分析。需要确定是专注于单一功能还是做综合平台。
- **备选方案**：
  - 方案A：预订取消预测系统（核心：二分类预测，机器学习任务明确）
  - 方案B：综合分析仪表盘（核心：全面分析，包含预测+分群+多维度统计）
  - 方案C：价格优化分析（核心：回归预测房价，商业价值导向）
- **对比维度**：问题定义清晰度、数据集适配度、实时流处理场景适配度、工作量、4周时间可行性
- **最终决策**：选择方案A（预订取消预测系统）+ 扩展实时统计和多维度分析。理由：数据集天然有 is_canceled 标签，问题定义清晰（二分类），符合实时流处理场景（预订逐条产生→实时预测），工作量适中。同时扩展实时统计、趋势分析、多维度分析功能，避免功能过于单一。放弃方案B因为工作量可能超出4周，放弃方案C因为缺少明确的价格标签。

**关键确认**：用户担心方案是否符合 Flume+Kafka+Storm 要求，经过分析确认完全符合：
- 数据流场景：模拟预订实时产生（CSV逐行读取）→ Flume采集 → Kafka缓冲 → Storm实时处理
- Storm作用：不仅做预测，还做实时统计聚合（滑动窗口计算取消率、热门地区等）
- 业务价值：帮助酒店提前识别高风险预订，优化资源分配

**状态**：已确定核心功能方向

---

### [MAJOR] 技术实现方案选择
**操作**：提出3种不同的技术实现方案，对比开发难度、时间成本、功能完整性

**决策过程**：
- **问题分析**：课程要求必须整合 Flume+Kafka+Storm，但这些框架配置复杂。需要在"真实使用大数据框架"和"开发效率"之间找到平衡。
- **备选方案**：
  - 方案A：完整企业级方案（真实安装配置 Flume+Kafka+Storm，Linux服务器部署）
  - 方案B：Docker容器化方案（Docker Compose一键部署大数据框架，Windows/Mac均可）
  - 方案C：本地模拟方案（Python multiprocessing模拟流处理，无需配置大数据环境）
- **对比维度**：环境搭建难度、调试便利性、是否真实使用大数据框架、答辩说服力、时间成本、学习价值
- **最终决策**：推荐方案B（Docker容器化方案）。理由：平衡了开发效率和技术完整性，真实运行大数据框架但环境搭建简单（1天内完成），4周时间充裕，答辩时可现场演示完整数据流，Docker是必备技能顺便学会。放弃方案A因为环境搭建耗时1-2周容易卡在配置问题，放弃方案C因为只是模拟答辩时可能被质疑。

**状态**：已提出技术方案，等待用户选择

---

### [MAJOR] 确定项目名称
**操作**：为项目起名，经过多轮讨论最终确定名称

**决策过程**：
- **问题分析**：初始名称强调"实时"，但用户质疑酒店预订取消预测是否真的需要实时响应。经过讨论，明确课程要求的"实时"是指使用实时流处理框架（流式 vs 批处理），而非业务上的毫秒级响应。预订预测的业务价值是"自动化、持续监控"，而非"立即响应"。
- **关键约束**：
  1. 必须体现"可视化"（这是《数据可视化》课程）
  2. 不过度强调"实时"（避免误导）
  3. 体现核心功能（分析+预测）
- **备选方案**：
  - 方案一：酒店预订数据可视化分析与预测系统
  - 方案二：基于大数据的酒店预订可视化分析平台
  - 方案三：酒店预订智能可视化决策系统
  - 方案四：酒店预订取消预测可视化平台
- **对比维度**：是否突出"可视化"、功能描述完整性、名称长度、专业性
- **最终决策**：选择方案一（酒店预订数据可视化分析与预测系统）。理由："可视化"放在核心位置符合课程定位，"分析与预测"体现完整功能（不只是预测还有多维度分析），答辩时更有说服力。

**项目名称**：酒店预订数据可视化分析与预测系统
**英文名**：Hotel Booking Data Visualization Analysis & Prediction System
**简称**：HBDVAP System

**状态**：已确定项目名称

---

### [MAJOR] 开发环境选择决策（修正）
**操作**：讨论开发工具和大数据框架部署方式

**决策过程**：
- **问题分析**：用户上学期配置过 Storm，但不记得安装位置，也不确定 Kafka 和 Flume 是否配置。尝试查找 Storm 时命令被 hook 误拦截。用户不喜欢 Jupyter Notebook，平时用 Claude 写 Python。
- **备选方案**：
  - 方案A：重新手动安装配置 Flume+Kafka+Storm（耗时1-2周，但用户可能更熟悉传统方式）
  - 方案B：Docker 一键部署（学习成本低，但用户明确拒绝）
  - 方案C：继续找旧的 Storm，如果找到就补充 Kafka+Flume（时间不确定）
  - 方案D：Python 模拟流处理（最快但不符合课程要求）
- **对比维度**：时间成本、学习难度、是否符合课程要求、用户接受度
- **最终决策**：用户明确拒绝 Docker 方案。需要在方案A（重新安装）和方案C（找旧环境）之间选择。

**开发工具调整**：
- 去掉 Jupyter Notebook（用户不喜欢）→ 改用普通 Python 脚本
- 去掉 VS Code 要求 → 用户用 Claude Code CLI + 任意编辑器
- 大数据框架部署：待定（不用 Docker）

**状态**：等待用户选择方案A或方案C

---

### [MINOR] 查找旧的大数据框架环境
**操作**：使用 bash 命令在 C 盘和 D 盘查找 Flume、Kafka、Storm 的安装目录

**原因**：用户上学期配置过 Storm，希望复用旧环境以节省配置时间。

**遇到的错误与修复**：
1. 第一次尝试使用 PowerShell 语法（Get-ChildItem）但执行环境是 bash，导致命令失败
2. 修复方法：改用 bash 语法（find 命令）重新查找

**查找结果**：
- C 盘根目录及子目录（深度2层）：未找到相关目录
- D 盘根目录及子目录（深度2层）：未找到相关目录
- PATH 环境变量：未找到相关路径

**结论**：旧的大数据框架环境可能已被删除、移动到更深层级目录，或安装在其他盘符。建议直接重新安装 Flume+Kafka+Storm。

**状态**：查找完成，未找到旧环境，建议重新安装

---

### [MAJOR] 找到旧的 Storm 环境并确定补充方案
**操作**：用户提示在 `D:\huqianyang\流计算stom` 目录下有旧的 Storm 环境，检查其完整性并确定补充方案

**发现**：
- **Storm 1.2.3**：位于 `D:\huqianyang\流计算stom\apache-storm-1.2.3`，目录结构完整（bin、conf、lib等）
- **之前的项目**：`idea_storm_program` 包含 `storm-wordcount` 项目，说明之前做过词频统计实验
- **配置文件**：storm.yaml 存在，但关键配置（zookeeper、nimbus）被注释，说明之前是单机模式
- **Kafka**：未找到
- **Flume**：未找到

**决策过程**：
- **问题分析**：找到了可用的 Storm 环境，但缺少 Kafka 和 Flume。课程要求必须整合 Flume+Kafka+Storm 三个框架。需要决定是复用 Storm 并补充其他两个，还是全部重新安装。
- **备选方案**：
  - 方案A：复用现有 Storm 1.2.3 + 补充安装 Kafka 和 Flume（节省时间，只需配置2个组件）
  - 方案B：全部重新安装最新版本（版本统一，但耗时更长）
  - 方案C：使用 Docker（用户已明确拒绝）
- **对比维度**：时间成本、版本兼容性、配置复杂度、是否符合课程要求
- **最终决策**：选择方案A（复用 Storm + 补充 Kafka 和 Flume）。理由：Storm 1.2.3 是稳定版本，可以正常使用，只需补充 Kafka 和 Flume 即可满足课程要求。预计配置时间1-2天（vs 全部重新安装需要2-3天）。版本兼容性方面，Storm 1.2.3 可以与 Kafka 2.x 和 Flume 1.9 良好配合。

**下一步行动**：
1. 补充安装 Kafka（推荐 2.8.0 版本）
2. 补充安装 Flume（推荐 1.9.0 版本）
3. 配置三者集成（Flume → Kafka → Storm）
4. 测试数据流是否正常

**状态**：已确定复用 Storm 并补充 Kafka 和 Flume

---

### [MAJOR] 明确项目角色与协作模式
**操作**：用户明确自己的角色和项目完成方式

**关键信息**：
- 用户是成员A（数据与算法工程师）
- 用户是组长，需要确定整个项目的大框架和细节
- 用户可以一个人完成ABC三个角色的所有工作

**决策影响**：
- 项目结构需要适合单人开发或小组协作两种模式
- 大数据框架需要在用户本地完整搭建（因为用户要负责整合）
- 需要完整的项目规划和开发顺序
- Git仓库需要支持组员协作（如果有组员加入）

**状态**：已明确角色，需要调整项目方案

---

## 2026-05-26

### [MAJOR] 评估组内启动包发送方式
**操作**：分析是否应一次性把所有文档打包发给成员 B/C，以及该启动包应包含哪些部分。

**原因**：用户希望判断豆包给出的组长执行建议是否适合当前项目，并进一步明确应该给组员发哪些文件、按什么顺序发，避免后续分工混乱。

**决策过程**：
- **问题分析**：当前项目已经确定题目、数据集与核心方向，但组员几乎零基础，只会使用 Claude/Codex 等 AI 工具。若直接把零散文档发群，容易出现字段、接口、目录不一致；若一次性发太多，也会让组员不知道先看什么。
- **备选方案**：
  1. 一次性发全部文档：信息完整，但阅读负担大、优先级不清晰。
  2. 分阶段发核心规范+AI提示词：先统一字段/接口/目录，再发各自任务提示词，最便于并行开发。
  3. 等成员各自开始后再补发文档：协调成本最低，但会拖慢启动节奏，且容易前后不一致。
- **对比维度**：启动速度、统一性、返工风险、组员理解成本、是否适合零基础团队。
- **最终决策**：推荐方案2，采用“统一启动包”但分层发放：先发总说明、字段规范、接口文档、目录结构，再发成员 A/B/C 各自的 AI 提示词与任务边界。这样既能快速启动，又能保证所有人按同一标准生成代码。

**状态**：已完成方案评估，待继续细化启动包清单与分工说明。

---

### [MINOR] 创建启动包整理任务
**操作**：新增任务，用于后续继续收敛微信启动包内容和发送顺序。

**原因**：当前工作已经从“判断是否合适”推进到“如何组织成可直接发送的材料”，需要单独记录以便后续继续细化。

**状态**：已创建任务，等待继续整理。

---

### [MAJOR] 收紧过程文件追加规则
**操作**：根据用户反馈，明确过程文件必须增量追加，不能整文件覆盖。

**原因**：用户指出 `progress.md` 已存在，若直接重写会覆盖历史记录，破坏阶段决策和时间线。

**决策过程**：
- **问题分析**：当前流程虽然要求每次修改后更新 `progress.md`，但未明确“只能追加不能覆盖”。这会导致误操作整文件重写，丢失历史记录。
- **备选方案**：
  1. 不改规则，继续依赖人工注意：实现简单，但容易再次误覆盖。
  2. 仅约束 `progress.md`：能缓解问题，但 `task_plan.md`、`findings.md` 仍可能被同类误操作覆盖。
  3. 统一约束所有过程文件：要求修改前先读取现有内容，且只能在原有基础上追加或局部修补。
- **对比维度**：防误覆盖能力、适用范围、执行复杂度、是否符合过程文件用途。
- **最终决策**：采用方案3，统一把 `progress.md`、`task_plan.md`、`findings.md` 视为“只追加不覆盖”的过程记录，修改前必须先读取现有内容，默认增量更新。

**状态**：已明确规则方向，后续按增量方式维护过程文件。

---

### [MINOR] 恢复过程文件上下文时命令语法错误
**操作**：按照文件规划系统 skill 要求检查未同步会话上下文时，尝试执行 Windows PowerShell 格式的 `session-catchup.py` 命令。

**原因**：当前工具实际执行环境是 bash，不支持 PowerShell 的 `& (Get-Command ...)` 语法，导致命令报错。该错误只影响自动恢复检查，不影响已读取的 `task_plan.md`、`findings.md`、`progress.md` 内容，也不影响后续对启动包方案的分析。

**修复方向**：后续如需继续运行该脚本，应改用 bash 可执行的 Python 调用方式，或直接读取已有过程文件恢复上下文。

**状态**：已识别错误原因，继续基于已读取过程文件推进启动包方案分析。

---

### [MINOR] 创建启动包任务清单
**操作**：根据 `/goal` 指令，将“按照统一启动包思路完成”拆成启动包目录、核心文档、成员任务与 AI 提示词、过程文件同步四个任务。

**原因**：本次工作包含多个文档和协作交付物，需要先建立可跟踪任务，避免直接散乱生成文件，也便于后续确认哪些启动包内容已完成。

**状态**：已创建任务清单，开始创建启动包目录结构。

---

### [MINOR] 创建启动包文档目录
**操作**：创建 `00_project_docs/` 和 `07_ai_prompts/` 两个目录，用于存放发给组员的项目说明、规范文档和 AI 提示词。

**原因**：当前项目根目录已有若干文件，但缺少统一的启动包目录。单独创建文档目录可以避免 B/C 组员直接面对散乱文件，也便于微信打包发送。

**状态**：目录已创建，下一步编写核心启动文档。

---

### [MINOR] 编写启动包总说明与目录结构
**操作**：新增 `00_project_docs/项目总说明.md` 和 `00_project_docs/项目目录结构.md`。

**原因**：启动包需要先让 B/C 明确项目是什么、技术路线是什么、各自负责哪些目录，以及哪些规范不能私自改。总说明解决“看不懂项目”的问题，目录结构解决“各写各的最后合不起来”的问题。

**状态**：两个核心文档已创建，继续编写字段规范、接口文档、数据流架构和开发时间表。

---

### [MINOR] 恢复过程文件读取参数错误
**操作**：继续 `/goal` 后恢复过程文件时，第一次并行读取 `task_plan.md`、`findings.md`、`progress.md` 误传了空 `pages` 参数，随后改为 `pages: "1"` 成功读取。

**原因**：Read 工具只有读取 PDF 时才需要页码参数，普通 Markdown 文件传空字符串会触发参数校验错误。该错误没有修改任何项目文件，只影响一次读取尝试。

**状态**：已用正确参数恢复过程文件和任务状态，继续编写启动包核心文档。

---

### [MINOR] 编写字段规范与接口文档
**操作**：新增 `00_project_docs/字段规范.md` 和 `00_project_docs/接口文档.md`。

**原因**：B/C 都主要依赖 AI 工具开发，最容易出现字段名、接口地址、JSON 返回格式不一致的问题。字段规范用于统一数据、模型、后端、前端使用的字段；接口文档用于固定 Flask API 契约，避免前后端联调时互相改。

**状态**：字段规范和接口文档已创建，继续编写数据流架构说明和开发时间表。

---

### [MINOR] 编写数据流架构说明与开发时间表
**操作**：新增 `00_project_docs/数据流架构说明.md` 和 `00_project_docs/开发时间表.md`。

**原因**：老师要求使用 Flume、Kafka、Storm，因此启动包必须明确大数据组件在系统中的正式位置，不能只作为可选装饰；开发时间表用于让 B/C 按阶段推进，避免零基础成员拿到文档后不知道先做什么。

**状态**：核心启动文档已基本完成，下一步编写成员 A/B/C 任务说明与 B/C 可复制给 AI 的提示词。

---

### [MINOR] 编写小组分工说明与成员A提示词
**操作**：新增 `00_project_docs/小组分工说明.md` 和 `07_ai_prompts/成员A_数据算法提示词.md`。

**原因**：用户是组长兼成员 A，需要明确自己、B、C 的职责边界，并保留自己后续进行数据清洗、特征工程、模型训练时可直接复制给 AI 的提示词。

**状态**：A 的任务边界和提示词已完成，继续编写成员 B/C 的 AI 提示词。

---

### [MINOR] 编写成员B/C提示词
**操作**：新增 `07_ai_prompts/成员B_后端大数据提示词.md` 和 `07_ai_prompts/成员C_前端可视化提示词.md`。

**原因**：B/C 组员主要依赖 Claude、Codex、豆包等 AI 工具开发，直接给任务描述仍可能生成不兼容代码；统一提示词可以强制 AI 按项目目录、字段规范和接口文档生成内容。

**状态**：成员 A/B/C 的任务说明和提示词已完成，下一步同步 `task_plan.md`、`findings.md` 并输出启动包总结。

---

### [MINOR] 同步启动包阶段过程文件
**操作**：更新 `task_plan.md` 和 `findings.md`，补充启动包文档阶段的完成清单、阶段结论和下一步建议。

**原因**：启动包文档已经基本完成，需要把当前阶段状态同步到过程文件，确保后续继续开发时能从过程文件恢复上下文，而不是依赖对话记忆。

**状态**：`task_plan.md` 与 `findings.md` 已同步，准备输出本阶段完成总结。

---

### [MINOR] 重写 README 为项目入口说明
**操作**：将 `README.md` 从旧的详细方案文档改为项目入口说明，统一指向 `00_project_docs/` 和 `07_ai_prompts/`。

**原因**：原 README 中的目录结构、文档路径和技术栈与新启动包结构不一致，容易导致 B/C 按旧目录生成代码。新版 README 明确声明详细协作规范以 `00_project_docs/` 为准，AI 提示词以 `07_ai_prompts/` 为准，并使用新的 `00/01/02/03/04/05/06/07` 目录结构。

**状态**：README 已完成重写，当前与启动包文档职责不冲突。

---

### [MINOR] 检查 Git 初始化前状态
**操作**：执行 `git status --short` 检查当前目录是否已有 Git 仓库，并检查是否存在 `.gitignore`。

**原因**：用户要求按照建议建立 Git 仓库，初始化前需要确认当前目录状态，避免重复初始化或误提交未规划文件。

**检查结果**：当前目录还不是 Git 仓库；未发现 `.gitignore` 文件。

**状态**：准备创建 `.gitignore` 并初始化本地 Git 仓库。

---

### [MINOR] 创建 Git 忽略规则
**操作**：新增 `.gitignore`，忽略 Python 缓存、虚拟环境、IDE 文件、日志、模型输出、数据输出目录和根目录数据集 `hotel_bookings.csv`。

**原因**：当前项目包含数据集和后续会生成的模型文件，直接提交可能导致仓库过大或混入临时产物。先建立忽略规则可以保证 Git 主要管理代码、文档和配置。

**状态**：`.gitignore` 已创建，下一步初始化 Git 仓库。

---

### [MINOR] 初始化 Git 仓库
**操作**：执行 `git init`，在当前课程设计目录下创建本地 Git 仓库，并查看未跟踪文件。

**原因**：项目已进入小组协作和多文档阶段，需要使用 Git 记录版本，防止 README、字段规范、接口文档、过程文件等被误覆盖后无法恢复。

**检查结果**：Git 仓库已初始化；当前未跟踪内容包括 `.claude/`、`.gitignore`、`00_project_docs/`、`07_ai_prompts/`、`README.md`、过程文件和老师要求文档目录；根目录数据集 `hotel_bookings.csv` 已被 `.gitignore` 忽略。

**状态**：准备按文件白名单暂存项目文档、配置和过程记录。

---

### [MINOR] 检查提交前 Git 状态
**操作**：执行 `git status --short`、`git diff` 和 `git log --oneline -5` 检查提交前状态。

**原因**：创建首个提交前需要确认哪些文件会进入版本管理，并查看仓库是否已有提交历史。

**检查结果**：`git status` 显示当前文档、配置和过程文件均为未跟踪文件；`git diff` 对未跟踪文件无输出；`git log` 报错 `fatal: your current branch 'master' does not have any commits yet`，这是新初始化仓库尚无提交的正常现象。

**状态**：继续准备首个提交，只暂存文档、配置和过程文件，不提交数据集。

---

### [MINOR] 暂存首批项目文件
**操作**：暂存 `.gitignore`、`README.md`、过程文件、`.claude/settings.json`、`00_project_docs/`、`07_ai_prompts/` 和老师要求文档目录。

**原因**：这些文件构成当前项目启动包和协作规则，应纳入 Git 首个版本；根目录数据集 `hotel_bookings.csv` 已通过 `.gitignore` 排除，避免仓库过大。

**检查结果**：Git 提示部分文本文件会在 Windows 下从 LF 转为 CRLF，这是换行符提示；同时发现 `.claude/settings.json.backup-*` 仍为未跟踪文件，需要加入忽略规则，避免误提交备份配置。

**状态**：准备更新 `.gitignore` 忽略 `.claude` 备份文件。

---

### [MINOR] 忽略 Claude 配置备份文件
**操作**：更新 `.gitignore`，新增 `.claude/*.backup-*` 忽略规则。

**原因**：`.claude/settings.json.backup-*` 是配置备份文件，不属于项目正式配置；提交它们会增加噪音，也可能让后续判断当前生效配置时混淆。

**状态**：忽略规则已更新，需要重新暂存 `.gitignore` 和最新 `progress.md`。

---

### 待办事项
- [x] 用户下载酒店预订数据集
- [x] 数据探索性分析（EDA）
- [x] 确定具体功能方向（取消预测+实时统计+多维度分析）
- [x] 确定项目名称
- [x] 提出技术实现方案
- [x] 调整开发工具（去掉Jupyter，拒绝Docker）
- [x] 找到旧的Storm环境
- [x] 明确项目角色（组长，可独立完成）
- [x] 评估是否适合一次性发送启动包
- [ ] 确定项目目录结构（适合单人/小组协作）
- [ ] 初始化Git仓库
- [ ] 补充安装Kafka和Flume
- [ ] 整理启动包文件清单（总说明/规范/提示词/附录）
- [ ] 明确成员 A/B/C 的职责边界
- [ ] 输出可直接微信发送的消息模板
- [ ] 如需，补充字段规范与接口规范的骨架
- [ ] 完成系统设计文档
- [ ] 编写详细实现计划
